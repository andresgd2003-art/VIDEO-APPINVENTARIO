[pytest]
# Agrega src/ al sys.path ANTES de que pytest importe cualquier test.
pythonpath = src
testpaths = tests
markers =
    slow: test lento (OCR / modelos pesados)
    integration: test de integración end-to-end
